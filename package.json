{
  "name": "masthead-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Masthead — landing page + waitlist (static site with a Vercel serverless route).",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev"
  },
  "engines": {
    "node": ">=18"
  }
}
